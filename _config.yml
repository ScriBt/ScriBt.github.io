name: Projekt ScriBt
description: The ROM building automation tool
markdown: kramdown
exclude:
  - Gemfile
  - Gemfile.lock
defaults:
  -
    scope:
      path: "wiki_pages"
    values:
      type: wiki
      layout: wiki
  -
    scope:
      path: "_posts"
    values:
      type: post
      layout: post
